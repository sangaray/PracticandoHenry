// Helper: es una función o conjunto de funciones que realizan una tarea específica que no necesariamente va a ser usada en otros proyectos o en varios lugares del proyecto actual

module.exports = (n) => {
  return `${n} años`;
};
