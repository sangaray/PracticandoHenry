import React from "react";

// styles
import styles from "./index.module.css";

const Button: React.FC = () => {
  return <button className={styles.button}>Botón</button>;
};

export default Button;
